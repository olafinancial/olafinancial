# Copy this to .env.test and fill in your test credentials.
# NEVER commit .env.test to git.

BASE_URL=http://localhost:3000
TEST_EMAIL=testuser@example.com
TEST_PASSWORD=TestPassword123!

# Optional: point to a separate Supabase test project to keep prod data clean
SUPABASE_URL=https://YOUR_TEST_PROJECT.supabase.co
SUPABASE_ANON_KEY=your_test_anon_key
