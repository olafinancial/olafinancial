import { chromium } from 'playwright'; const browser = await chromium.launch(); const context = await browser.newContext(); const page = await context.newPage(); page.on('console', msg => console.log('BROWSER CONSOLE:', msg.type(), msg.text())); page.on('pageerror', err => console.log('BROWSER EXCEPTION:', err.message, err.stack)); await page.goto('http://localhost:3000/#/login'); await page.fill('#login-email', 'testuser@example.com'); await page.fill('#login-password', 'TestPassword123!'); await page.click('#login-btn'); await page.waitForTimeout(5000); console.log('URL after login:', page.url()); console.log('Clicking Income sidebar link...'); await page.click('#nav-income'); await page.waitForTimeout(3000); console.log('URL after click:', page.url()); await browser.close();
